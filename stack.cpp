#include "stack.h"
#include <fstream>

stack::stack(){
  width = 0;
  height = 0;
  max_color = 0; 
}

void stack::readFile(std::string name, int amount){

  int i = 0;
  ifstream file;
  while(amount > 0){
    file.open()

  }
}
